from setuptools import setup, find_packages

setup(name='netgan_without_gan', 
      version='0.1',
      description='NetGAN without GAN: From Random Walks to Low-Rank Approximations', 
      packages=find_packages())
